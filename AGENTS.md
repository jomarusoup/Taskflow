# task_flow — 프로젝트 규약

> 모든 CLI(Claude·Codex·Gemini·Copilot)가 공유하는 **프로젝트 지침 정본(SSOT)**.
> 전역 공통 규약은 Arachne(`~/.claude/rules/`·AGENTS.md)가 이미 커버한다 — 여기에는
> **이 프로젝트에서만 유효한 것**만 적는다.
>
> 기록 원칙: ① 코드·git에서 파생 가능한 정보는 적지 않는다(코드가 정본).
> ② 매 세션 로드되는 파일이므로 줄 하나가 반복 비용이다 — 비자명한 것만 채운다.
> ③ 전체 기획은 `docs/plan/PLAN.md`, 진행 중 작업·결정 이력은 `docs/task/`·`docs/issue/`에
>    적는다(여긴 규약만).

## 구조

<!-- 미기재: 주요 디렉터리·모듈 역할. 코드만 봐서는 비자명한 것만 -->

## 빌드·검증

<!-- 미기재: 빌드·테스트·실행 명령. .arachne/commands 와 중복이면 생략 -->

## grep 키워드 매핑

<!-- 미기재: 기능 → 파일·심볼 키워드. 이슈 착수 시 sgrep 진입점 -->

## 학습된 패턴

<!-- 미기재: /learn 으로 누적. 같은 실수 2회 이상 반복 등 비자명 패턴만 -->
