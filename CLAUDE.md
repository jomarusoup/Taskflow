# task_flow — Claude Code 보충

프로젝트 규약 정본은 @AGENTS.md 를 본다.

Claude 전용 보충(훅·슬래시 커맨드·에이전트 라우팅 등 다른 CLI와 무관한 것)이
필요해지면 이 파일에 추가한다 — 공통 규약은 AGENTS.md 한 곳만 고친다.

## 확장 도구 (설치된 경우)

조사·영향 분석 `/codegraph`, 코드 구조 시각화·온보딩 `/understand`, 프론트엔드 UI `taste-skill`.
미설치면 `arachne --extras` (전역 1회 설치 → 모든 프로젝트 사용).
